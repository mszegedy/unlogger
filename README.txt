Does the opposite of keylogging: it reads off lines from a file, and types them
out when prompted by the capslock key. Quits when the Pause key is pressed or
when it runs out of lines. Only works on Linux with evdev drivers. Requires the
Python library `evdev`.
